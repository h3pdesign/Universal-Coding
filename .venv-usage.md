Shared Python environment for this repository
==========================================

Create or activate the workspace virtual environment:

```bash
cd /Users/h3p/Coding/Universal-Coding
source .venv/bin/activate
```

Verify it:

```bash
python --version
pip --version
```

Use this environment for Python scripts, notebooks, and experiments in this repo.
